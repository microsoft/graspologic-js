import { RenderConfiguration, RenderConfigurationOptions } from './types';
/**
 * @internal
 *
 * Creates a new render configuration
 * @param props The partial set of render configuration options
 */
export declare function createConfiguration(props?: Partial<RenderConfigurationOptions>): RenderConfiguration;
